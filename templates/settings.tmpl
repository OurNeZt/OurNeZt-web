{{ define "settings" }}
{{ template "partials/header" . }}
<section class="card bg-base-100 shadow max-w-2xl">
  <div class="card-body">
    <h1 class="card-title">Settings</h1>
    <p class="opacity-70">Phase 1 placeholder for user settings and account preferences.</p>
  </div>
</section>
{{ template "partials/footer" . }}
{{ end }}
